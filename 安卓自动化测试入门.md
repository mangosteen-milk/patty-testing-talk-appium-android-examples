## 安卓自动化入门

#### 写脚本前的准备工作

- 具备一定的编程基础。
- 已安装安卓自动化测试环境

#### 工具和编程语言建议

- **开发工具：PyCharm**。https://www.jetbrains.com/pycharm/download/
- **编程语言：Python**。https://www.python.org/
- **查找元素的工具：Appium Inspector**。https://github.com/appium/appium-inspector

#### 操作步骤

- **把adb和aapt工具的路径添加到环境变量Path中**

  - Appium可以通过adb去给手机下达一些命令，比如说安装被测App之类的。

  - aapt工具是Android Studio自带的，可方便地获取到安装包的package和activity信息。通常位于Android SDK目录下的build-tools下的某个版本中，可以选目前目录里的最高版本的目录，添加到Path环境变量中。不同Android Studio版本可能有差异

- **在PyCharm新建一个测试项目，建议使用Python虚拟环境**。

- **在Python虚拟环境中安装Appium client库**。
  
  - 执行命令安装：`pip install Appium-Python-Client`
  
- **安装完成后，把手机开启USB调试**。
  
  - 如果使用安卓模拟器，则不需要设置USB调试
  - 如果使用安卓真机，则需要开启USB调试，把手机连接到电脑。开启USB调试方法如下：
    1. 点击系统的设置App → 关于手机 → 连续点击"版本号"7次，可开启开发者模式(不同厂商手机可能不一样，但基本上差不太多)
    2. 点击设置App → 开发者选项 → 开启"USB调试"
  
- **开始写代码**。可复制官网的示例进行改写

```python
import unittest
from appium import webdriver
from appium.options.android import UiAutomator2Options
from appium.webdriver.common.appiumby import AppiumBy

capabilities = dict(
    platformName='Android',
    automationName='uiautomator2',
    deviceName='Android',
    appPackage='com.android.settings',
    appActivity='.Settings',
    language='en',
    locale='US'
)

appium_server_url = 'http://localhost:4723'

class TestAppium(unittest.TestCase):
    def setUp(self) -> None:
        self.driver = webdriver.Remote(appium_server_url, options=UiAutomator2Options().load_capabilities(capabilities))

    def tearDown(self) -> None:
        if self.driver:
            self.driver.quit()

    def test_find_battery(self) -> None:
        el = self.driver.find_element(by=AppiumBy.XPATH, value='//*[@text="Battery"]')
        el.click()

if __name__ == '__main__':
    unittest.main()
```

- **获取被测应用的package和activity信息**。

  ```python
  # 使用aapt获取安卓安装包的package和activity
  aapt dump badging AndroidDemo.apk | grep "package"
  aapt dump badging AndroidDemo.apk | grep "activity"
  ```
  
- **设置相应的参数**

  ```python
  import os
  capabilities = dict(
      platformName='Android',
      automationName='uiautomator2',
      deviceName='Android',
    	app=f'{os.getcwd()}/app/AndroidDemo.apk',
    	noReset=True,
      appPackage='com.example.androiddemo',
      appActivity='com.example.androiddemo.MainActivity',
  )
  ```

- **使用工具Appium Inspector查找元素**

  国产手机首次做安卓自动化，可能会遇到如下错误，其实就是appium在对手机执行某些命令的时候权限不足

  ```
  Exception occurred while executing 'delete':
  java.lang.SecurityException: Permission denial, must have one of: [android.permission.WRITE_SECURE_SETTINGS]
  ```

  设置->搜索“禁止权限监控”，点击相关的搜索结果，然后开启“禁止权限监控”的选项

- 在代码中查找元素，对元素进行操作

  ```
  ...
  ele = driver.find_element(...)
  ele.click()
  ```

- 执行测试，看测试结果是否通过